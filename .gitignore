# [Terraform] --------------------------------------------------
# 테라폼 실행 시 생성되는 로컬 작업 폴더 (의존성 파일들)
.terraform/

# 로컬 상태 파일 (우리는 S3 Backend를 쓰므로 로컬 파일은 절대 금지!)
*.tfstate
*.tfstate.backup
*.tfstate.lock.info

# 변수 정의 파일 (비밀번호나 개인 설정이 들어있을 수 있음)
*.tfvars
*.tfvars.json

# 테라폼 실행 로그
*.log